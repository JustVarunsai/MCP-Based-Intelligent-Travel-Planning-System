# Railway deployment for the MCP server (and backend, separate service)
# Real values filled in Phase 11.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m mcp_server.server --sse --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
