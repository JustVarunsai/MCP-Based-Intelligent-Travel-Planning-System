# Frontend (Next.js 15 + Tailwind + shadcn/ui)

This folder is scaffolded in Phase 6.

To bootstrap manually:
```bash
cd ..
npx create-next-app@latest frontend --typescript --tailwind --app --src-dir --import-alias "@/*"
```
